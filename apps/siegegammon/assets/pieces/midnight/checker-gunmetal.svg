<?xml version="1.0" encoding="UTF-8"?>
<svg
  xmlns="http://www.w3.org/2000/svg"
  xmlns:inkscape="http://www.inkscape.org/namespaces/inkscape"
  xmlns:sodipodi="http://sodipodi.sourceforge.net/DTD/sodipodi-0.0.dtd"
  viewBox="0 0 28 28"
  width="28"
  height="28">

  <sodipodi:namedview inkscape:document-units="px" />

  <defs>
    <!-- Face radial gradient -->
    <radialGradient id="gunmetal-face" cx="0.4" cy="0.35" r="0.55">
      <stop offset="0%" stop-color="#3a3e48" />
      <stop offset="50%" stop-color="#252830" />
      <stop offset="100%" stop-color="#14161a" />
    </radialGradient>

    <!-- Edge radial gradient for depth -->
    <radialGradient id="gunmetal-edge" cx="0.5" cy="0.5" r="0.5">
      <stop offset="75%" stop-color="#14161a" />
      <stop offset="100%" stop-color="#000000" />
    </radialGradient>

    <!-- Drop shadow filter -->
    <filter id="gunmetal-shadow" x="-10%" y="-10%" width="130%" height="130%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="1" result="blur" />
      <feOffset in="blur" dx="1" dy="1" result="shadow" />
      <feMerge>
        <feMergeNode in="shadow" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>
  </defs>

  <!-- Checker layer -->
  <g inkscape:groupmode="layer" inkscape:label="Checker">
    <!-- Edge / base circle -->
    <circle cx="14" cy="14" r="12" fill="url(#gunmetal-edge)" filter="url(#gunmetal-shadow)" />

    <!-- Face -->
    <circle cx="14" cy="14" r="11" fill="url(#gunmetal-face)" />

    <!-- Inner decorative ring -->
    <circle cx="14" cy="14" r="9" fill="none" stroke="#505560" stroke-width="0.75" />

    <!-- Highlight ellipse -->
    <ellipse cx="11" cy="11" rx="4" ry="3" fill="white" opacity="0.12" />
  </g>
</svg>
